import { SignInBlock } from '../../components/blocks';

export const LoginPage = () => {
  return (
    <div className="container mx-auto">
      <h1>Login Page</h1>
      <SignInBlock />
    </div>
  );
};
